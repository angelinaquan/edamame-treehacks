# Marketing site

The public URL **https://angelinaquan.github.io/edamame/** is built from GitHub Pages on the companion repo **[angelinaquan/edamame](https://github.com/angelinaquan/edamame)** (`/docs` on `main`).

After editing `index.html` here, copy it to that repo’s `docs/index.html` and push so the live site updates. (This copy stays in sync with the app monorepo for convenience.)
